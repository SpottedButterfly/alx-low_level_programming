#include "main.h"

/**
 * main - takes pointer to int as parameter
 * and updates value it points to 98
 *
 * Return: 0 if successsful
 */

int main(void)
{
	int n = 98;

	reset_to_98(&n);
	return (0);
}
